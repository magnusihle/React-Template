import { createBrowserHistory } from 'history';
//  Creating browserhistory this can be used to update the browser history anywhere by using "histpry.push"
export const history = createBrowserHistory();

